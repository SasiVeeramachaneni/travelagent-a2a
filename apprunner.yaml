version: 1.0
runtime: python311
build:
  commands:
    build:
      - echo "Build phase - dependencies will be installed at runtime"
run:
  command: bash start.sh
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"
    - name: APP_VERSION
      value: "1.0.0"
