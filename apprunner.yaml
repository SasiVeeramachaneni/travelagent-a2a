version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt
run:
  command: python3 -m uvicorn app:app --host 0.0.0.0 --port $PORT
  network:
    port: 8080
  env:
    - name: A2A_PUBLIC_HOST
      value: "${AWS_APP_RUNNER_SERVICE_URL}"
    - name: APP_VERSION
      value: "1.0.0"
